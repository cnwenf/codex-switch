# codex-switch Product

## Register

product

## Users

主要用户是需要在 Codex 中同时使用官方订阅模型和多个第三方 Responses 兼容模型的开发者。他们在本机配置 API Provider、选择模型、检查连接状态，并把生成的模型目录应用到 Codex。用户希望这套工具安装后即可工作，不需要理解代理内部实现，也不需要手写和反复核对多份配置。

## Product Purpose

codex-switch 是一个仅监听本机回环地址的薄代理和配置工具。它按模型 ID 把 Codex 请求路由到正确的上游，除认证头外不改写请求或响应。配置页负责安全地管理 Provider、校验 API Key、发现模型与能力、生成并备份 Codex 配置。成功标准是用户能清楚判断某个厂商是否可直连、凭据是否有效、模型能力是否已确认，并能在不暴露凭据或破坏原配置的情况下完成切换。

## Brand Personality

克制、可信、技术清晰。界面应像成熟的本地开发工具：信息密度适中，状态可解释，操作结果直接，不使用营销式措辞或装饰性视觉制造虚假的智能感。

## Anti-references

- 不做炫技式 AI 控制台，不使用霓虹渐变、玻璃拟态、夸张动效或无意义的大指标。
- 不把不兼容的厂商伪装成可用，也不把未知能力显示为明确支持或不支持。
- 不把 API Key、上游错误正文或内部堆栈回显到页面。
- 不用只能依赖鼠标或颜色理解状态的自定义控件。

## Design Principles

1. 先说明兼容边界，再提供操作。unsupported、limited、beta 和验证状态必须清楚区分。
2. 自动化过程可见。Key 校验、模型发现、能力来源、缓存新鲜度和失败回退都应有明确状态。
3. 安全默认值优先。固定厂商 URL 由服务端生成，Custom 才允许编辑 URL，敏感字段不回显。
4. 熟悉的控件优先。厂商和模型使用支持搜索与键盘操作的标准组合框，保留清晰的保存、取消和错误反馈。
5. 保留用户控制。自动发现的模型可以选择和编辑，旧配置可继续使用，历史与备份入口保持可见。

## Accessibility & Inclusion

面向桌面浏览器，按 WCAG 2.1 AA 的基本要求实现。所有表单控件需有可读标签、明显焦点态和键盘路径；搜索下拉框支持方向键、Enter 和 Escape；加载、成功、警告、失败不能只靠颜色表达；尊重减少动态效果设置；中英文模型名和较长 URL 不能破坏布局。
